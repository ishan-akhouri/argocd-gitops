image:
  repository: ishanakhouri/argo-cd-app
  tag: "latest"
  pullPolicy: Always

replicaCount: 2

service:
  type: ClusterIP
  port: 3000

imagePullSecrets:
  - name: regcred

rollout:
  enabled: true
  canarySteps:
    - setWeight: 20
    - pause:
        duration: 2m
